apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: workshop
    component: frontend
spec:
  type: NodePort
  selector:
    app: workshop
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
